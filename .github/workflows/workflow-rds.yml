# name: 'backend_rds/*'
on:
  push:
    paths:
      - 'backend_rds/*'


jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with: 
          fetch-depth: 0